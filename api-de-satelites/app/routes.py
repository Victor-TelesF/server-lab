# TODO: implemente aqui as rotas (APIRouter).
#
# Rotas sugeridas pra esse exercício:
# - POST   /satelites             -> cria um satélite
# - GET    /satelites/{id}        -> busca um satélite
# - PATCH  /satelites/{id}/status -> aplica uma transição (ativar/desativar/manutencao)
#
# from fastapi import APIRouter, Depends
# from sqlalchemy.orm import Session
# from app.database import get_db
#
# router = APIRouter(prefix="/satelites", tags=["satelites"])
